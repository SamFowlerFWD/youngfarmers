import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Norfolk YFC - Do More, Be More with Norfolk Young Farmers",
  description: "Join Norfolk Federation of Young Farmers Clubs. Meet new people, learn skills, travel the world, and make a difference in your community. Ages 10-28. You don't have to be a farmer to join!",
  keywords: "Norfolk YFC, Young Farmers, Norfolk, Youth Club, Agriculture, Community, Membership",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
